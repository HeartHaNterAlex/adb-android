cd adb-android

chmod +x termux-adb.sh
./termux-adb.sh

# fastboot-mode-run ComMand

./fb.sh

# check device name in number

# last command ran first copy recovery location

termux-fastboot flash recovery
